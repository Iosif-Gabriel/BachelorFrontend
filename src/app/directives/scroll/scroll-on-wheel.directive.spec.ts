import { ScrollOnWheelDirective } from './scroll-on-wheel.directive';

describe('ScrollOnWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollOnWheelDirective();
    expect(directive).toBeTruthy();
  });
});
